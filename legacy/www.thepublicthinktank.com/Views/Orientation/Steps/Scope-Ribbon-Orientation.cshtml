Click on it to toggle the Composite Scope view.
When the card is in a minimized view the Composite Scope is in a scrollable container.
But if you expand the card, you can view the entire scope without scrolling.

The idea behind scope.